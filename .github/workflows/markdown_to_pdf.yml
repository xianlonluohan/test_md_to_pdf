name: "Markdown to PDF Converter with Tiled Watermark"

on:
  push:
    branches: ["main", "master"]
    tags: ["v*", "pdf-*"]
  pull_request:
    branches: ["main", "master"]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  convert-md-to-pdf:
    runs-on: ubuntu-latest
    
    steps:
      - name: "Checkout repository"
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: "Install dependencies"
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            pandoc \
            texlive-xetex \
            texlive-latex-extra \
            texlive-fonts-recommended \
            texlive-latex-recommended \
            texlive-lang-chinese \
            fonts-noto-cjk \
            fonts-noto-cjk-extra \
            fonts-wqy-microhei \
            fonts-wqy-zenhei \
            lmodern \
            tree \
            jq \
            python3-pip \
            poppler-utils  # 添加PDF工具
          
          pip3 install beautifulsoup4
      
      - name: "Preprocess Markdown files - Clean up HTML links"
        run: |
          echo "清理Markdown文件中的HTML链接，确保正确转换为Markdown格式..."
          
          # 创建预处理脚本
          cat > preprocess_links.py << 'EOF'
          import os
          import re
          import sys
          
          def clean_html_links(content):
              """清理HTML链接，确保正确转换为Markdown格式"""
              
              # 保护代码块
              code_blocks = []
              def store_code_block(match):
                  code_blocks.append(match.group(0))
                  return f'@@@CODE_BLOCK_{len(code_blocks)-1}@@@'
              
              content = re.sub(r'```[\s\S]*?```', store_code_block, content)
              content = re.sub(r'~~~[\s\S]*?~~~', store_code_block, content)
              
              # 处理所有<a>标签 - 更健壮的正则表达式
              # 匹配 <a href="..." [other attributes]>text</a>
              a_pattern = re.compile(r'<a\s+([^>]+)>([^<]+)</a>', re.IGNORECASE | re.DOTALL)
              
              def replace_a_tag(match):
                  attrs_str = match.group(1)
                  link_text = match.group(2).strip()
                  
                  # 提取href属性，忽略其他属性如download, target等
                  href_match = re.search(r'href\s*=\s*["\']([^"\']+)["\']', attrs_str, re.IGNORECASE)
                  if not href_match:
                      return match.group(0)  # 如果没有href，保持原样
                  
                  href = href_match.group(1)
                  
                  # 清理href
                  href = re.sub(r'^\./', '', href)
                  href = re.sub(r'^/', '', href)
                  
                  # 返回纯净的Markdown链接
                  return f'[{link_text}]({href})'
              
              content = a_pattern.sub(replace_a_tag, content)
              
              # 处理图片标签
              img_pattern = re.compile(r'<img\s+([^>]+)>', re.IGNORECASE)
              
              def replace_img_tag(match):
                  attrs_str = match.group(1)
                  
                  src_match = re.search(r'src\s*=\s*["\']([^"\']+)["\']', attrs_str)
                  alt_match = re.search(r'alt\s*=\s*["\']([^"\']*)["\']', attrs_str)
                  
                  src = src_match.group(1) if src_match else ''
                  alt = alt_match.group(1) if alt_match else ''
                  
                  if src:
                      src = re.sub(r'^\./', '', src)
                      src = re.sub(r'^/', '', src)
                  
                  return f'![{alt}]({src})'
              
              content = img_pattern.sub(replace_img_tag, content)
              
              # 移除center标签但保留内容
              content = re.sub(r'<center>(.*?)</center>', r'\1', content, flags=re.DOTALL | re.IGNORECASE)
              
              # 移除其他HTML标签（保留基本标签）
              content = re.sub(r'<(?!\/?(?:a|img|center|br|p|div|span|table|tr|td|th|ul|ol|li|strong|em|b|i|u|h[1-6]|code|pre)\b)[^>]+>', '', content, flags=re.IGNORECASE)
              
              # 恢复代码块
              for i, code_block in enumerate(code_blocks):
                  placeholder = f'@@@CODE_BLOCK_{i}@@@'
                  content = content.replace(placeholder, code_block)
              
              return content
          
          # 处理所有Markdown文件
          for root, dirs, files in os.walk('.'):
              # 跳过特定目录
              skip_dirs = ['.git', 'pdf_output', '__pycache__', '.github', 'node_modules']
              dirs[:] = [d for d in dirs if d not in skip_dirs]
              
              for file in files:
                  if file.endswith('.md'):
                      filepath = os.path.join(root, file)
                      try:
                          with open(filepath, 'r', encoding='utf-8', errors='ignore') as f:
                              content = f.read()
                          
                          # 清理HTML链接
                          cleaned_content = clean_html_links(content)
                          
                          # 写回文件
                          with open(filepath, 'w', encoding='utf-8') as f:
                              f.write(cleaned_content)
                          
                          print(f"已处理: {filepath}")
                      except Exception as e:
                          print(f"处理失败 {filepath}: {e}")
          
          print("链接清理完成")
          EOF
          
          python3 preprocess_links.py
      
      - name: "Create LaTeX template"
        run: |
          cat > header.tex << 'EOF'
          \usepackage{xeCJK}
          \usepackage[UTF8]{ctex}
          \setCJKmainfont{Noto Serif CJK SC}
          \setCJKsansfont{Noto Sans CJK SC}
          \setCJKmonofont{Noto Sans Mono CJK SC}
          
          \usepackage[margin=1.2in]{geometry}
          
          \usepackage{hyperref}
          \hypersetup{
            colorlinks=true,
            linkcolor=blue,
            urlcolor=blue,
            citecolor=blue,
            pdfborder={0 0 0},
            pdftitle={},
            pdfauthor={},
            pdfsubject={},
            pdfkeywords={},
            pdfcreator={LaTeX with hyperref package},
            pdfproducer={xelatex},
            bookmarks=true,
            bookmarksopen=true,
            bookmarksnumbered=true,
            pdfstartview=FitH,
            pdfpagemode=UseOutlines,
          }
          
          \usepackage{listings}
          \usepackage{xcolor}
          \usepackage{graphicx}
          \usepackage{float}
          \usepackage{placeins}
          \usepackage{booktabs}
          \usepackage{array}
          \usepackage{caption}
          \usepackage{etoolbox}
          
          \definecolor{codebg}{rgb}{0.98,0.98,0.98}
          \definecolor{framecolor}{rgb}{0.8,0.8,0.8}
          
          % 代码块设置 - 保持尖括号为黑色
          \lstset{
            basicstyle=\ttfamily\normalsize,
            backgroundcolor=\color{codebg},
            frame=single,
            framesep=6pt,
            framerule=0.8pt,
            rulecolor=\color{framecolor},
            breaklines=true,
            breakatwhitespace=false,
            breakautoindent=true,
            breakindent=0pt,
            postbreak=\raisebox{0ex}[0ex][0ex]{\ensuremath{\color{red}\hookrightarrow\space}},
            showstringspaces=false,
            tabsize=2,
            numbers=none,
            xleftmargin=12pt,
            xrightmargin=12pt,
            upquote=true,
            columns=fullflexible,
            keepspaces=true,
            % 移除尖括号的颜色设置，保持默认黑色
          }
          
          \usepackage{parskip}
          \setlength{\parindent}{0pt}
          \setlength{\parskip}{0.8em}
          
          \sloppy
          \emergencystretch=3em
          
          \usepackage{eso-pic}
          \usepackage{tikz}
          \usetikzlibrary{calc}
          
          % 图片浮动控制
          \usepackage{float}
          \usepackage{placeins}
          \let\origfigure\figure
          \let\endorigfigure\endfigure
          \renewenvironment{figure}[1][]{
            \origfigure[H]
          }{
            \endorigfigure
          }
          
          % 防止图片和表格过度浮动
          \renewcommand{\topfraction}{.85}
          \renewcommand{\bottomfraction}{.7}
          \renewcommand{\textfraction}{.15}
          \renewcommand{\floatpagefraction}{.66}
          \setcounter{topnumber}{9}
          \setcounter{bottomnumber}{9}
          \setcounter{totalnumber}{20}
          \setcounter{dbltopnumber}{9}
          
          % 去掉图片标题
          \usepackage{caption}
          \captionsetup[figure]{
            labelformat=empty,
            textformat=empty,
            justification=centering,
            singlelinecheck=true,
            labelsep=none,
            skip=0pt
          }
          
          \usepackage{etoolbox}
          \AtBeginEnvironment{figure}{%
            \captionsetup{labelformat=empty,textformat=empty}%
          }
          
          % 表格设置
          \usepackage{booktabs}
          \usepackage{array}
          \newcolumntype{L}{>{\raggedright\arraybackslash}p{0.2\textwidth}}
          \newcolumntype{C}{>{\centering\arraybackslash}p{0.3\textwidth}}
          \newcolumntype{R}{>{\raggedleft\arraybackslash}p{0.2\textwidth}}
          
          % 水印设置
          \newcommand\BackgroundPicture{%
            \begin{tikzpicture}[remember picture,overlay]
              \coordinate (page center) at (current page.center);
              \pgfmathsetmacro{\pagewidth}{\paperwidth}
              \pgfmathsetmacro{\pageheight}{\paperheight}
              
              \pgfmathsetmacro{\hcount}{3}
              \pgfmathsetmacro{\vcount}{5}
              \pgfmathsetmacro{\hspace}{\paperwidth/(\hcount+1)}
              \pgfmathsetmacro{\vspace}{\paperheight/(\vcount+1)}
              
              \foreach \i in {1,...,\hcount} {
                \foreach \j in {1,...,\vcount} {
                  \pgfmathsetmacro{\x}{-\paperwidth/2 + \i*\hspace}
                  \pgfmathsetmacro{\y}{-\paperheight/2 + \j*\vspace}
                  
                  \node at ($(page center) + (\x pt, \y pt)$) [
                    rotate=45,
                    scale=1.5,
                    text=gray!30,
                    opacity=0.4,
                    font=\sffamily\bfseries\small
                  ] {emakefun};
                }
              }
              
              \node at (page center) [
                rotate=45,
                scale=3,
                text=gray!30,
                opacity=0.6,
                font=\sffamily\bfseries
              ] {emakefun};
            \end{tikzpicture}%
          }
          
          \AddToShipoutPictureBG{\BackgroundPicture}
          
          % 图片处理设置
          \graphicspath{{./}{./picture/}{./resource/}}
          
          % 设置图片最大宽度为行宽
          \makeatletter
          \def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
          \makeatother
          
          \let\oldincludegraphics\includegraphics
          \renewcommand{\includegraphics}[2][]{%
            \oldincludegraphics[#1,width=\maxwidth]{#2}%
          }
          EOF
      
      - name: "Set up output directory"
        run: |
          mkdir -p pdf_output
      
      - name: "Convert Markdown to PDF - Simple method"
        run: |
          MD_FILES=$(find . -name "*.md" -not -path "./.*" -not -path "*/node_modules/*" -not -path "./pdf_output/*" | sed 's/^\.\///')
          
          IFS=$'\n'
          converted_count=0
          failed_count=0
          
          for md_file in $MD_FILES; do
            [ -z "$md_file" ] && continue
            
            echo "========================================"
            echo "处理文件: $md_file"
            echo "========================================"
            
            dir_name=$(dirname "$md_file")
            base_name=$(basename "$md_file" .md)
            
            mkdir -p "pdf_output/$dir_name"
            
            md_dir=$(dirname "$md_file")
            
            # 构建资源路径
            resource_paths="$md_dir:$md_dir/picture:$md_dir/../picture:.:./picture:./resource"
            
            echo "资源路径: $resource_paths"
            
            set +e
            
            echo "开始转换PDF..."
            
            # 方法1: 使用简单的Pandoc转换，不添加额外参数
            echo "使用方法1: 简单Pandoc转换..."
            
            # 使用简单的Pandoc命令，避免链接被转换为脚注
            pandoc "$md_file" \
              --resource-path="$resource_paths" \
              -o "pdf_output/$dir_name/${base_name}_method1.pdf" \
              --pdf-engine=xelatex \
              --include-in-header=header.tex \
              -V mainfont="Noto Serif CJK SC" \
              -V sansfont="Noto Sans CJK SC" \
              -V monofont="Noto Sans Mono CJK SC" \
              -V geometry:margin=1.2in \
              -V geometry:a4paper \
              -V colorlinks=true \
              -V linkcolor=blue \
              -V urlcolor=blue \
              --wrap=auto \
              -f markdown-auto_identifiers \
              -t latex \
              --standalone \
              2>&1 | tee conversion.log
            
            # 方法2: 使用listings包和更简单的设置
            echo "使用方法2: 带listings的转换..."
            
            # 创建简化版header
            cat > header_simple.tex << 'EOF'
          \usepackage{xeCJK}
          \usepackage[UTF8]{ctex}
          \setCJKmainfont{Noto Serif CJK SC}
          \setCJKsansfont{Noto Sans CJK SC}
          \setCJKmonofont{Noto Sans Mono CJK SC}
          
          \usepackage[margin=1.2in]{geometry}
          
          \usepackage{hyperref}
          \hypersetup{
            colorlinks=true,
            linkcolor=blue,
            urlcolor=blue,
            citecolor=blue,
            pdfborder={0 0 0},
          }
          
          \usepackage{listings}
          \usepackage{xcolor}
          \usepackage{graphicx}
          \usepackage{float}
          
          \definecolor{codebg}{rgb}{0.98,0.98,0.98}
          \definecolor{framecolor}{rgb}{0.8,0.8,0.8}
          
          \lstset{
            basicstyle=\ttfamily\normalsize,
            backgroundcolor=\color{codebg},
            frame=single,
            framesep=6pt,
            framerule=0.8pt,
            rulecolor=\color{framecolor},
            breaklines=true,
            showstringspaces=false,
            tabsize=2,
            numbers=none,
          }
          
          \usepackage{eso-pic}
          \usepackage{tikz}
          \usetikzlibrary{calc}
          
          \newcommand\BackgroundPicture{%
            \begin{tikzpicture}[remember picture,overlay]
              \coordinate (page center) at (current page.center);
              \pgfmathsetmacro{\pagewidth}{\paperwidth}
              \pgfmathsetmacro{\pageheight}{\paperheight}
              
              \pgfmathsetmacro{\hcount}{3}
              \pgfmathsetmacro{\vcount}{5}
              \pgfmathsetmacro{\hspace}{\paperwidth/(\hcount+1)}
              \pgfmathsetmacro{\vspace}{\paperheight/(\vcount+1)}
              
              \foreach \i in {1,...,\hcount} {
                \foreach \j in {1,...,\vcount} {
                  \pgfmathsetmacro{\x}{-\paperwidth/2 + \i*\hspace}
                  \pgfmathsetmacro{\y}{-\paperheight/2 + \j*\vspace}
                  
                  \node at ($(page center) + (\x pt, \y pt)$) [
                    rotate=45,
                    scale=1.5,
                    text=gray!30,
                    opacity=0.4,
                    font=\sffamily\bfseries\small
                  ] {emakefun};
                }
              }
              
              \node at (page center) [
                rotate=45,
                scale=3,
                text=gray!30,
                opacity=0.6,
                font=\sffamily\bfseries
              ] {emakefun};
            \end{tikzpicture}%
          }
          
          \AddToShipoutPictureBG{\BackgroundPicture}
          
          \graphicspath{{./}{./picture/}{./resource/}}
          EOF
            
            pandoc "$md_file" \
              --resource-path="$resource_paths" \
              -o "pdf_output/$dir_name/${base_name}_method2.pdf" \
              --pdf-engine=xelatex \
              --include-in-header=header_simple.tex \
              -V mainfont="Noto Serif CJK SC" \
              -V sansfont="Noto Sans CJK SC" \
              -V monofont="Noto Sans Mono CJK SC" \
              -V geometry:margin=1.2in \
              -V geometry:a4paper \
              -V colorlinks=true \
              -V linkcolor=blue \
              -V urlcolor=blue \
              -f markdown \
              -t latex \
              --standalone \
              2>&1 | tee -a conversion.log
            
            # 选择最好的结果
            if [ -f "pdf_output/$dir_name/${base_name}_method1.pdf" ] && [ -s "pdf_output/$dir_name/${base_name}_method1.pdf" ]; then
              # 检查method1是否有链接
              if pdftotext "pdf_output/$dir_name/${base_name}_method1.pdf" - 2>/dev/null | grep -q "https://"; then
                echo "✓ 方法1成功且有链接"
                mv "pdf_output/$dir_name/${base_name}_method1.pdf" "pdf_output/$dir_name/$base_name.pdf"
                rm -f "pdf_output/$dir_name/${base_name}_method2.pdf"
              else
                if [ -f "pdf_output/$dir_name/${base_name}_method2.pdf" ] && [ -s "pdf_output/$dir_name/${base_name}_method2.pdf" ]; then
                  echo "✓ 使用方法2的结果"
                  mv "pdf_output/$dir_name/${base_name}_method2.pdf" "pdf_output/$dir_name/$base_name.pdf"
                else
                  echo "✓ 使用方法1的结果（无链接但文件存在）"
                  mv "pdf_output/$dir_name/${base_name}_method1.pdf" "pdf_output/$dir_name/$base_name.pdf"
                fi
              fi
            elif [ -f "pdf_output/$dir_name/${base_name}_method2.pdf" ] && [ -s "pdf_output/$dir_name/${base_name}_method2.pdf" ]; then
              echo "✓ 使用方法2的结果"
              mv "pdf_output/$dir_name/${base_name}_method2.pdf" "pdf_output/$dir_name/$base_name.pdf"
            else
              echo "✗ 两种方法都失败"
              failed_count=$((failed_count + 1))
              rm -f conversion.log 2>/dev/null || true
              continue
            fi
            
            rm -f "pdf_output/$dir_name/${base_name}_method1.pdf" "pdf_output/$dir_name/${base_name}_method2.pdf" 2>/dev/null || true
            
            set -e
            
            # 检查最终PDF
            if [ -f "pdf_output/$dir_name/$base_name.pdf" ] && [ -s "pdf_output/$dir_name/$base_name.pdf" ]; then
              echo "✓ 成功转换: $md_file"
              converted_count=$((converted_count + 1))
              
              pdf_size=$(du -h "pdf_output/$dir_name/$base_name.pdf" | cut -f1)
              echo "PDF文件大小: $pdf_size"
              
              # 验证PDF内容和链接
              echo "检查PDF内容..."
              
              # 提取文本查看是否有链接
              pdf_text=$(pdftotext "pdf_output/$dir_name/$base_name.pdf" - 2>/dev/null || echo "")
              
              if echo "$pdf_text" | grep -q -E 'https?://'; then
                echo "✓ PDF中包含URL链接"
              else
                echo "⚠ PDF中可能不包含URL链接（或格式不同）"
              fi
              
              # 检查PDF是否可以正常打开
              if pdftoppm "pdf_output/$dir_name/$base_name.pdf" -png -singlefile -f 1 -l 1 "pdf_output/$dir_name/${base_name}_preview" 2>/dev/null; then
                echo "✓ PDF可以正常渲染"
                rm -f "pdf_output/$dir_name/${base_name}_preview-1.png" 2>/dev/null || true
              fi
            else
              echo "✗ 最终PDF文件不存在或为空"
              failed_count=$((failed_count + 1))
            fi
            
            rm -f conversion.log 2>/dev/null || true
            rm -f header_simple.tex 2>/dev/null || true
            
            echo ""
          done
          
          echo "========================================"
          echo "转换汇总: $converted_count 成功, $failed_count 失败"
          echo "========================================"
          
          if [ $converted_count -eq 0 ] && [ $failed_count -gt 0 ]; then
            echo "所有转换都失败了!"
            exit 1
          fi
      
      - name: "Copy resource directories to output"
        run: |
          echo "复制resource目录到输出目录..."
          
          find pdf_output -name "*.pdf" -type f | while read pdf_file; do
            pdf_dir=$(dirname "$pdf_file")
            rel_dir=${pdf_dir#pdf_output/}
            
            if [ -d "$rel_dir/resource" ]; then
              echo "复制 $rel_dir/resource 到 $pdf_dir/"
              cp -r "$rel_dir/resource" "$pdf_dir/" 2>/dev/null || echo "无法复制 $rel_dir/resource"
            fi
          done
      
      - name: "Verify PDF files"
        run: |
          total_pdfs=$(find pdf_output -name "*.pdf" -type f 2>/dev/null | wc -l)
          
          if [ $total_pdfs -eq 0 ]; then
            echo "没有生成PDF文件!"
            exit 1
          else
            echo "总共生成 $total_pdfs 个PDF文件"
            
            total_size_mb=$(find pdf_output -name "*.pdf" -type f -exec stat -c%s {} \; | awk '{sum+=$1} END {print int(sum/1024/1024)}')
            echo "PDF文件总大小: ${total_size_mb}MB"
            
            echo "前10个最大的PDF文件:"
            find pdf_output -name "*.pdf" -type f -exec du -h {} \; | sort -rh | head -10
            
            echo ""
            echo "检查几个示例PDF的链接状态:"
            sample_pdfs=$(find pdf_output -name "*.pdf" -type f | head -3)
            for pdf in $sample_pdfs; do
              echo "检查 $pdf:"
              if pdftotext "$pdf" - 2>/dev/null | grep -q -E 'https?://'; then
                echo "  ✓ 包含URL链接"
              else
                # 检查是否有Markdown格式的链接
                if pdftotext "$pdf" - 2>/dev/null | grep -q '\[.*\](.*)'; then
                  echo "  ⚠ 包含Markdown格式链接但不一定是可点击的"
                else
                  echo "  ⚠ 可能没有超链接"
                fi
              fi
            done
          fi
      
      - name: "Create release documentation"
        run: |
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            TAG_NAME="${GITHUB_REF#refs/tags/}"
          else
            TAG_NAME="latest"
          fi
          
          echo "# PDF 文档集" > pdf_output/README.md
          echo "" >> pdf_output/README.md
          echo "生成时间: $(date '+%Y-%m-%d %H:%M:%S')" >> pdf_output/README.md
          echo "已优化HTML链接处理，避免链接被转换为脚注" >> pdf_output/README.md
          echo "代码块带边框，自动换行" >> pdf_output/README.md
          echo "添加平铺式 'emakefun' 水印" >> pdf_output/README.md
          echo "包含resource目录中的程序文件" >> pdf_output/README.md
          echo "优化图片位置控制，确保图片在正确位置" >> pdf_output/README.md
          echo "支持表格内图片正常显示" >> pdf_output/README.md
          echo "图片下方不显示任何标题标签（完全去掉'图x：'）" >> pdf_output/README.md
          echo "代码块中尖括号保持黑色显示" >> pdf_output/README.md
          echo "修复链接转换问题，正确显示可点击的蓝色链接" >> pdf_output/README.md
          echo "使用预处理步骤确保HTML链接正确转换为Markdown格式" >> pdf_output/README.md
          echo "" >> pdf_output/README.md
          
          echo "## 文件列表" >> pdf_output/README.md
          echo "" >> pdf_output/README.md
          
          PDF_COUNT=$(find pdf_output -name "*.pdf" -type f 2>/dev/null | wc -l)
          
          current_dir=""
          find pdf_output -name "*.pdf" -type f | sort | while read pdf_file; do
            rel_path=${pdf_file#pdf_output/}
            dir_name=$(dirname "$rel_path")
            file_name=$(basename "$rel_path")
            file_size=$(du -h "$pdf_file" | cut -f1)
            
            if [ "$current_dir" != "$dir_name" ]; then
              current_dir="$dir_name"
              if [ "$dir_name" != "." ]; then
                echo "### $dir_name/" >> pdf_output/README.md
                echo "" >> pdf_output/README.md
              fi
            fi
            
            pdf_dir=$(dirname "$pdf_file")
            resource_exists=""
            if [ -d "$pdf_dir/resource" ]; then
              resource_exists=" (含resource资源)"
            fi
            
            echo "- [$file_name]($rel_path) ($file_size)$resource_exists" >> pdf_output/README.md
          done
          
          echo "" >> pdf_output/README.md
          total_size_mb=$(find pdf_output -name "*.pdf" -type f -exec stat -c%s {} \; | awk '{sum+=$1} END {print int(sum/1024/1024)}')
          
          resource_dirs_count=$(find pdf_output -type d -name "resource" 2>/dev/null | wc -l)
          
          echo "总计: $PDF_COUNT 个 PDF 文件, 总大小约 ${total_size_mb}MB, ${resource_dirs_count} 个目录包含resource资源" >> pdf_output/README.md
      
      - name: "Create zip archive for release"
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          TAG_NAME="${GITHUB_REF#refs/tags/}"
          
          cd pdf_output
          zip -r "../pdf-documents-$TAG_NAME.zip" .
          cd ..
      
      - name: "Upload PDF artifacts"
        uses: actions/upload-artifact@v4
        with:
          name: pdf-documents
          path: |
            pdf_output/
            pdf-documents-*.zip
          retention-days: 7
      
      - name: "Create GitHub Release"
        if: startsWith(github.ref, 'refs/tags/')
        id: create_release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          name: "PDF Documents - ${{ github.ref_name }}"
          body_path: pdf_output/README.md
          files: |
            pdf-documents-${{ github.ref_name }}.zip
          draft: false
          prerelease: false
          generate_release_notes: true